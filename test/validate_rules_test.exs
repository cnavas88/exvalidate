defmodule Exvalidate.ValidateTest do
  use ExUnit.Case
  doctest Exvalidate

  # alias Exvalidate.Validate

  test "EEEE" do
    #     filed :: "id"
    # rules :: %{"required" => true}
    # data :: %{"id" => "123"}
    # result :: {:cont, {:ok, %{"id" => "123"}}}
    # RESULT :: {:ok, %{"id" => "123"}}
    # result = Validate.rules()
  end
end
